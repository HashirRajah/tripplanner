import 'package:flutter/material.dart';

class TravelInfoScreen extends StatelessWidget {
  const TravelInfoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Travel info'),
      ),
    );
  }
}
